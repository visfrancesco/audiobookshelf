<div class="form-grid"><x-field name="username" label="Username" :value="$edit['username'] ?? ''" required /><x-field name="email" type="email" label="Email" :value="$edit['email'] ?? ''" /><x-field name="password" type="password" :label="$edit ? 'New password (leave blank to keep)' : 'Password'" minlength="12" autocomplete="new-password" :required="!$edit" /><label class="field"><span>Account type</span><select name="type">
@foreach(($edit['type'] ?? '') === 'root' ? ['root'] : ['user', 'admin', 'guest'] as $type)<option value="{{ $type }}" @selected(($edit['type'] ?? 'user') === $type)>{{ ucfirst($type) }}</option>
@endforeach</select></label></div><label class="check"><input type="checkbox" name="isActive" value="1" @checked($edit['isActive'] ?? true)>Account enabled</label><fieldset><legend>Permissions</legend><div class="form-grid">
@foreach(\App\Services\AdminForms::permissions() as $key => $label)<label class="check"><input type="checkbox" name="permissions[{{ $key }}]" value="1" @checked($edit['permissions'][$key] ?? in_array($key, ['download', 'accessAllLibraries', 'accessAllTags', 'accessExplicitContent']))>{{ $label }}</label>
@endforeach</div></fieldset><fieldset><legend>Allowed libraries when access to every library is disabled</legend>
@foreach($libraries as $library)<label class="check"><input type="checkbox" name="librariesAccessible[]" value="{{ $library['id'] }}" @checked(in_array($library['id'], $edit['librariesAccessible'] ?? []))>{{ $library['name'] }}</label>
@endforeach</fieldset><x-field name="itemTagsSelected" label="Selected tags (one per line)" type="textarea" rows="3" :value="implode("\n", $edit['itemTagsSelected'] ?? [])" />
